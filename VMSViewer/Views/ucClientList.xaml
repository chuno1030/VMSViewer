<UserControl x:Class="VMSViewer.ucDeviceList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VMSViewer"
             mc:Ignorable="d" 
             Height="1050" Width="250"
             Loaded="UserControl_Loaded">
    <Grid>
        <ScrollViewer x:Name="svDeviceList"
                      Background="{DynamicResource BackGround_Color}"
                      HorizontalScrollBarVisibility="Disabled">
            <ScrollViewer.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="그룹생성" Click="MenuItem_Click"/>
                </ContextMenu>
            </ScrollViewer.ContextMenu>

            <StackPanel x:Name="spDeviceGroupList" Orientation="Vertical">
                
            </StackPanel>
        </ScrollViewer>
    </Grid>
    
</UserControl>
